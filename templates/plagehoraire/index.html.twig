<!DOCTYPE html>
<html lang="en">

<!-- patients23:17-->
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=0">
    <link rel="stylesheet" type="text/css" href="assts/css/bootstrap.min.css">
    <script src="assts/js/jquery-3.2.1.min.js"></script>
    <script src="/assets/packages/core/index.global.min.js"></script>
    <script src="assets/packages/interaction/index.global.min.js"></script>
    <script src="/assets/packages/daygrid/index.global.min.js"></script> 
    <script src="/assets/packages/timegrid/index.global.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
    <script src=""></script>
    {#  <link rel="stylesheet" type="text/css" href="fullcalendar/main.css"> #}
    

    <!--[if lt IE 9]>
		<script src="assets/js/html5shiv.min.js"></script>
		<script src="assets/js/respond.min.js"></script>
	<![endif]-->
</head>
<body>
{% include "calendar/ajout.html.twig" %} 
<div id='calendar'></div>    
    <script>
     window.onload=()=> {
        var calendarEl = document.getElementById('calendar');
       
      // var modale=document.getElementById('')
       // var finalEvent=[];

        //events.forEach((el)=>{

           // finalEvent.push(
              //{
                //'id':el.id,
                //'title':el.title,
                //'start':new Date(el.start)
              //}
            //);

        //});
        //console.log(finalEvent);

        var calendar = new FullCalendar.Calendar(calendarEl, {
          initialView:'dayGridMonth',
          locale:'fr',
          timeZone:'Europe/Paris',
        headerToolbar: {
         start: 'prev,next today',
        center:'title',
        end :'dayGridMonth,timeGridWeek,timeGridDay'
      },
       //events:finalEvent,
       buttonText:{
        today:'AuJourd/hui',
        week:'Semaine',
        month:'Mois',  
       },
       editable:true,
       selectable:true,
     // dateClick: function(info) {
       // console.log('date Click' +info.dateStr)
          //var modal = document.getElementById("addEventModal");
          // $('modal').show();
       // $('#addEventModal').modal('show');
      // var title=document.getElementById('eventTitle').value=info.title;
      // var start=document.getElementById('eventStart').value=info.start;
       //var end=document.getElementById('eventEnd').value=info.end;
      // var patient=document.getElementById('eventPatient').value=info.patient;
      // var description=document.getElementById('eventDescription').value=info.description;
   //} 
});
     
calendar.render();
      };
       
    </script>




    {#  <script>
     $('#code_famille_vsm_fpc').on('change', function(){
                let me = $(this);
                $('#description_famille_vsm_fpc option[value="' + me.val() + '"]').prop('selected', true);

               // On va afficher les sous famille correspondantes à la famille selectionnée 

                let option_sf_default_value = $('#code_sous_famille_vsm_fpc option[value=""]');
                option_sf_default_value.prop('selected', true);

                let option_sf_all_value = $('#code_sous_famille_vsm_fpc option[value!=""]');
                option_sf_all_value.css('display', 'none');
                option_sf_all_value.attr('disabled', 'disabled');

                let option_sf_current_value = $('#code_sous_famille_vsm_fpc option[data-id-famille="' + me.val() + '"]');
                option_sf_current_value.css('display', '');
                option_sf_current_value.removeAttr('disabled');


                option_sf_default_value = $('#description_sous_famille_vsm_fpc option[value=""]');
                option_sf_default_value.prop('selected', true);

                option_sf_default_value = $('#description_sous_famille_vsm_fpc option[value=""]');

                option_sf_all_value = $('#description_sous_famille_vsm_fpc option[value!=""]');
                option_sf_all_value.css('display', 'none');
                option_sf_all_value.attr('disabled', 'disabled');

                option_sf_current_value = $('#description_sous_famille_vsm_fpc option[data-id-famille="' + me.val() + '"]');
                option_sf_current_value.css('display', '');
                option_sf_current_value.removeAttr('disabled');
            }); 
    
    
    </script> #}




</body>


<!-- patients23:19-->
</html>